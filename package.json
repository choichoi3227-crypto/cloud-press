{
  "name": "cloud-press",
  "version": "5.0.0",
  "description": "Cloudflare Workers 무료 검색 스크래핑 API — Google+Naver, API 키 없는 URL 요청 기반 엔드포인트",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:search": "wrangler deploy",
    "check": "node --check worker-search.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
