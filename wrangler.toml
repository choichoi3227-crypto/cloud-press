name = "cloudpress-search-endpoint"
main = "worker-search.js"
compatibility_date = "2026-05-14"

# Cloudflare Workers 무료 플랜 배포용 단독 검색 API입니다.
# 별도 DB, KV, R2, Secrets, API 키 없이 URL 요청만으로 동작합니다.
# 배포:
#   npx wrangler deploy
# 로컬 테스트:
#   npx wrangler dev

[observability]
enabled = false
